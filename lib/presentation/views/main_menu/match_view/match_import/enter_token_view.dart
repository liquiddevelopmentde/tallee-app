import 'package:flutter/material.dart';

class EnterTokenView extends StatelessWidget {
  const EnterTokenView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
